name: Creds

on:
  push

jobs:
 
  test:
    name: Show
    runs-on: ubuntu-latest
    
    env:
      WPT_REPORT_API_KEY: ${{ secrets.WPT_REPORT_API_KEY }}
      WPT_TEST_VALUE: Testwert

    steps:
      - name: Checkout repository
        uses: actions/checkout@8e5e7e5ab8b370d6c329ec480221332ada57f0ab # v3.5.2

      - name: Show env vars
        run: |
          echo "Hallo Welt" 
          echo $WPT_TEST_VALUE

